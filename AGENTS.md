# 项目约定

## 编写SKILL注意事项

- SKILL一般是通用性内容，描述内容中避免出现本机绝对路径。
- 优先使用中文，专属名词可保留英文。

## 临时文件

- 本项目是源码工程，工作过程生成的临时文件（脚本、数据等）禁止生成到本项目根目录下。
- 如果无可用目录，则脚本必须按功能或任务分类放入子目录（如 `temp_data/trade/`、`temp_data/odps/`），禁止直接放在 `temp_data/` 根目录下。

## 方案与计划编写

- 方案不得只描述起步版本，避免需要推倒重来的一次性临时代码。
- 用户提出的目标范围是最高优先级的完成标准。除非用户明确要求缩小范围，否则不得偷懒，擅自把目标改写为 Demo、第一版、最小推荐等类似目标版本。

# 代码编写

- 项目整体10%由人来制定框架，90%是通过AI来编写代码。
- 合理安排测试，避免改动少量代码即发起测试，将AI资源用在更有价值的地方。

